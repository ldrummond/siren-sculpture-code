# Copy this file to sync.env and set the Pi connection details.
PI_HOST=raspberrypi.local
PI_USER=admin
SSH_PORT=22
APP_DIR=/opt/sculpture

# Audio sync behavior. Initialization and deployment are prompted on each run.
SYNC_AUDIO=1
